#include<bits/stdc++.h>
using namespace std;
int main()
{
    string arr ="Pavankumar";
    // assesing the each element in array 
    // cout<<arr[0];
    cout<<arr<<endl;
    
    //printing string in vertical, to know the size of string we use arr.size()
    for(int i=0; i<arr.size(); i++)
    {
        cout<<arr[i]<<endl;
    }
    return 0;
}